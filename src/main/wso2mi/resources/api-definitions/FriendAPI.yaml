openapi: 3.0.1
info:
  title: FriendAPI
  description: API Definition of FriendAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/friend
- url: https://localhost:8253/friend
paths:
  /:
    get:
      responses:
        default:
          description: Default response
  /{name}:
    get:
      parameters:
      - name: name
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
